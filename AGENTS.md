# Repository Instructions

- 常に日本語で回答すること
- コードを追加または変更した場合は、日本語で簡潔なコメントを入れること
- コード中の不等号は、できるだけ `>` と `>=` を避けて `<` と `<=` の向きで記述すること
- XcodeGen の利用を禁止する
- `project.yml` や `project.yaml` などのプロジェクト生成設定を追加しないこと
- `.xcodeproj` と `.xcworkspace` は Xcode で直接作成・更新し、Xcode 管理で統一すること
- 指示がない限り、ビルド確認やコミットを行わないこと
